services:
  - type: web
    name: convertr-backend
    runtime: python
    buildCommand: apt-get install -y ffmpeg pandoc libreoffice && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT